-- Services
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local Lighting = game:GetService("Lighting")

local Player = Players.LocalPlayer
local Enemies = Workspace:WaitForChild("Enemies")
local Map = Workspace:WaitForChild("Map")

-- ===================================================
-- SEA DETECTION SYSTEM
-- ===================================================
local Sea1, Sea2, Sea3 = false, false, false
local PlaceId = game.PlaceId

if PlaceId == 2753915549 then
    Sea1 = true
    print("✓ Detected: First Sea (Sea 1)")
elseif PlaceId == 4442272183 then
    Sea2 = true
    print("✓ Detected: Second Sea (Sea 2)")
elseif PlaceId == 7449423635 then
    Sea3 = true
    print("✓ Detected: Third Sea (Sea 3)")
else
    -- Fallback detection
    if Map:FindFirstChild("Turtle") then
        Sea3 = true
    elseif Map:FindFirstChild("Ice Castle") then
        Sea2 = true
    else
        Sea1 = true
    end
    warn("Using fallback sea detection!")
end

-- Player Data
local PlayerLevel = Player:WaitForChild("Data"):WaitForChild("Level")
local MaxLavel = 2550
local IsOwner = false -- Set to true if you're the owner

print("Current Sea:", Sea1 and "Sea 1" or Sea2 and "Sea 2" or "Sea 3")
print("Player Level:", PlayerLevel.Value)

------Script Library--------

local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/jensonhirst/Orion/main/source')))()

local Window = OrionLib:MakeWindow({
    Name = "NFC Scripts : Blox Fruits",
    HidePremium = false,
    SaveConfig = false,
    ConfigFolder = "nfcscripts_BloxFruits"
})

-- ===================================================
-- FLOATING MINIMIZE BUTTON (FIXED VERSION)
-- ===================================================

local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local PlayerGui = Player:WaitForChild("PlayerGui")

-- Create ScreenGui for floating button
local FloatingGui = Instance.new("ScreenGui")
FloatingGui.Name = "FloatingButton"
FloatingGui.ResetOnSpawn = false
FloatingGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
FloatingGui.Parent = PlayerGui

-- Create the floating button
local FloatingButton = Instance.new("ImageButton")
FloatingButton.Name = "FloatingBtn"
FloatingButton.Size = UDim2.new(0, 60, 0, 60)
FloatingButton.Position = UDim2.new(0.9, 0, 0.1, 0)
FloatingButton.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
FloatingButton.BorderSizePixel = 0
FloatingButton.Image = "rbxassetid://3926305904"
FloatingButton.ImageColor3 = Color3.fromRGB(255, 255, 255)
FloatingButton.ScaleType = Enum.ScaleType.Fit
FloatingButton.Parent = FloatingGui

-- Rounded corners
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 12)
Corner.Parent = FloatingButton

-- Border stroke
local Stroke = Instance.new("UIStroke")
Stroke.Color = Color3.fromRGB(100, 100, 255)
Stroke.Thickness = 2
Stroke.Parent = FloatingButton

-- Shadow effect
local Shadow = Instance.new("ImageLabel")
Shadow.Name = "Shadow"
Shadow.Size = UDim2.new(1, 10, 1, 10)
Shadow.Position = UDim2.new(0, -5, 0, -5)
Shadow.BackgroundTransparency = 1
Shadow.Image = "rbxassetid://1316045217"
Shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
Shadow.ImageTransparency = 0.8
Shadow.ScaleType = Enum.ScaleType.Slice
Shadow.SliceCenter = Rect.new(10, 10, 118, 118)
Shadow.ZIndex = -1
Shadow.Parent = FloatingButton

-- ===== DRAGGING FUNCTIONALITY =====
local dragging
local dragInput
local dragStart
local startPos

local function update(input)
    local delta = input.Position - dragStart
    FloatingButton.Position = UDim2.new(
        startPos.X.Scale,
        startPos.X.Offset + delta.X,
        startPos.Y.Scale,
        startPos.Y.Offset + delta.Y
    )
end

FloatingButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = FloatingButton.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

FloatingButton.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

-- ===== TOGGLE UI FUNCTIONALITY (FIXED) =====
local UIVisible = true

-- Function to find Orion UI in both CoreGui and PlayerGui
local function getOrionUI()
    -- Try CoreGui first
    local coreGui = game:GetService("CoreGui")
    local orionUI = coreGui:FindFirstChild("Orion")
    if orionUI then
        return orionUI
    end
    
    -- Try PlayerGui
    orionUI = PlayerGui:FindFirstChild("Orion")
    if orionUI then
        return orionUI
    end
    
    return nil
end

FloatingButton.MouseButton1Click:Connect(function()
    UIVisible = not UIVisible
    
    -- Toggle Orion UI visibility (works for both CoreGui and PlayerGui)
    local OrionUI = getOrionUI()
    if OrionUI then
        if OrionUI:FindFirstChild("Main") then
            OrionUI.Main.Visible = UIVisible
        elseif OrionUI:IsA("ScreenGui") then
            OrionUI.Enabled = UIVisible
        end
    end
    
    -- Change button appearance
    if UIVisible then
        FloatingButton.ImageColor3 = Color3.fromRGB(255, 255, 255)
        Stroke.Color = Color3.fromRGB(100, 100, 255)
    else
        FloatingButton.ImageColor3 = Color3.fromRGB(150, 150, 150)
        Stroke.Color = Color3.fromRGB(255, 50, 50)
    end
    
    -- Show notification
    OrionLib:MakeNotification({
        Name = "UI Toggle",
        Content = UIVisible and "UI Shown" or "UI Hidden",
        Image = "rbxassetid://4483345998",
        Time = 2
    })
end)

-- ===== HOVER EFFECTS =====
FloatingButton.MouseEnter:Connect(function()
    TweenService:Create(FloatingButton, TweenInfo.new(0.2), {
        Size = UDim2.new(0, 70, 0, 70),
        BackgroundColor3 = Color3.fromRGB(45, 45, 60)
    }):Play()
end)

FloatingButton.MouseLeave:Connect(function()
    TweenService:Create(FloatingButton, TweenInfo.new(0.2), {
        Size = UDim2.new(0, 60, 0, 60),
        BackgroundColor3 = Color3.fromRGB(35, 35, 45)
    }):Play()
end)

-- ===== PULSING ANIMATION =====
task.spawn(function()
    while true do
        TweenService:Create(Stroke, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
            Thickness = 3
        }):Play()
        task.wait(1)
        TweenService:Create(Stroke, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
            Thickness = 2
        }):Play()
        task.wait(1)
    end
end)

-- ===== KEYBIND TO TOGGLE (Right Control) =====
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed then
        if input.KeyCode == Enum.KeyCode.RightControl then
            UIVisible = not UIVisible
            local OrionUI = getOrionUI()
            if OrionUI then
                if OrionUI:FindFirstChild("Main") then
                    OrionUI.Main.Visible = UIVisible
                elseif OrionUI:IsA("ScreenGui") then
                    OrionUI.Enabled = UIVisible
                end
            end
            FloatingButton.ImageColor3 = UIVisible and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(150, 150, 150)
            Stroke.Color = UIVisible and Color3.fromRGB(100, 100, 255) or Color3.fromRGB(255, 50, 50)
        end
    end
end)

print("✓ Floating button loaded! Click button or press Right Ctrl to toggle UI")

-- ===================================================
-- TABS INITIALIZATION
-- ===================================================

local AFKOptions = {}

-- Discord Tab
local Discord = Window:MakeTab({
    Name = "Discord",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

Discord:AddParagraph("Discord Community", "Join our discord community to receive information about the next update")
Discord:AddLabel("Discord: https://discord.gg/7aR7kNVt4g")

-- Main Farm Tab
local MainFarm = Window:MakeTab({
    Name = "Main Farm",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Stats Tab
local Stats = Window:MakeTab({
    Name = "Stats",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Players Tab
local PlayersTab = Window:MakeTab({
    Name = "Players",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Teleport Tab
local Teleport = Window:MakeTab({
    Name = "Teleport",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Misc Tab
local Misc = Window:MakeTab({
    Name = "Misc",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Shop Tab
local Shop = Window:MakeTab({
    Name = "Shop",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Visual Tab
local Visual = Window:MakeTab({
    Name = "Visual",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- ===================================================
-- HELPER FUNCTIONS
-- ===================================================

-- Function to safely fire remotes
local function FireRemote(...)
    local args = {...}
    pcall(function()
        ReplicatedStorage.Remotes.CommF_:InvokeServer(unpack(args))
    end)
end

-- Anti-AFK
Player.Idled:connect(function()
    VirtualUser:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
    wait(1)
    VirtualUser:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
end)

-- ===================================================
-- MAIN FARM TAB CONTENT
-- ===================================================

MainFarm:AddSection({Name = "Auto Farm Level"})

local AutoFarmLevel = false
local AutoFarmLevelConnection

MainFarm:AddToggle({
    Name = "Auto Farm Level",
    Default = false,
    Callback = function(Value)
        AutoFarmLevel = Value
        
        if Value then
            AutoFarmLevelConnection = RunService.Heartbeat:Connect(function()
                if AutoFarmLevel then
                    -- Add your auto farm logic here
                    pcall(function()
                        -- Example: Teleport to enemy and attack
                        for _, enemy in pairs(Enemies:GetChildren()) do
                            if enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health > 0 then
                                local player = Player.Character
                                if player and player:FindFirstChild("HumanoidRootPart") then
                                    player.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0, 10, 0)
                                    -- Add attack logic here
                                end
                                break
                            end
                        end
                    end)
                end
            end)
        else
            if AutoFarmLevelConnection then
                AutoFarmLevelConnection:Disconnect()
            end
        end
    end    
})

MainFarm:AddSection({Name = "Auto Farm Mastery"})

MainFarm:AddToggle({
    Name = "Auto Farm Mastery",
    Default = false,
    Callback = function(Value)
        -- Add mastery farm logic
    end    
})

-- ===================================================
-- STATS TAB CONTENT
-- ===================================================

Stats:AddSection({Name = "Auto Stats"})

Stats:AddToggle({
    Name = "Auto Melee",
    Default = false,
    Callback = function(Value)
        AFKOptions.Melee = Value
    end    
})

Stats:AddToggle({
    Name = "Auto Defense",
    Default = false,
    Callback = function(Value)
        AFKOptions.Defense = Value
    end    
})

Stats:AddToggle({
    Name = "Auto Sword",
    Default = false,
    Callback = function(Value)
        AFKOptions.Sword = Value
    end    
})

Stats:AddToggle({
    Name = "Auto Gun",
    Default = false,
    Callback = function(Value)
        AFKOptions.Gun = Value
    end    
})

Stats:AddToggle({
    Name = "Auto Fruit",
    Default = false,
    Callback = function(Value)
        AFKOptions.Fruit = Value
    end    
})

-- Stats Auto Upgrade Loop
task.spawn(function()
    while task.wait(0.1) do
        if AFKOptions.Melee then
            FireRemote("AddPoint", "Melee", 1)
        end
        if AFKOptions.Defense then
            FireRemote("AddPoint", "Defense", 1)
        end
        if AFKOptions.Sword then
            FireRemote("AddPoint", "Sword", 1)
        end
        if AFKOptions.Gun then
            FireRemote("AddPoint", "Gun", 1)
        end
        if AFKOptions.Fruit then
            FireRemote("AddPoint", "Demon Fruit", 1)
        end
    end
end)

-- ===================================================
-- PLAYERS TAB CONTENT
-- ===================================================

PlayersTab:AddSection({Name = "Player Options"})

PlayersTab:AddSlider({
    Name = "Walk Speed",
    Min = 16,
    Max = 300,
    Default = 16,
    Color = Color3.fromRGB(255,255,255),
    Increment = 1,
    ValueName = "Speed",
    Callback = function(Value)
        local character = Player.Character
        if character and character:FindFirstChild("Humanoid") then
            character.Humanoid.WalkSpeed = Value
        end
    end    
})

PlayersTab:AddSlider({
    Name = "Jump Power",
    Min = 50,
    Max = 500,
    Default = 50,
    Color = Color3.fromRGB(255,255,255),
    Increment = 1,
    ValueName = "Power",
    Callback = function(Value)
        local character = Player.Character
        if character and character:FindFirstChild("Humanoid") then
            character.Humanoid.JumpPower = Value
        end
    end    
})

-- ===================================================
-- TELEPORT TAB CONTENT
-- ===================================================

Teleport:AddSection({Name = "Island Teleports"})

local islandList = {}

if Sea1 then
    islandList = {
        "Middle Town",
        "Pirate Village",
        "Marine Base",
        "Jungle",
        "Desert",
        "Frozen Village",
        "Prison",
        "Magma Village",
        "Underwater City"
    }
elseif Sea2 then
    islandList = {
        "Kingdom of Rose",
        "Cafe",
        "Mansion",
        "Graveyard",
        "Snow Island",
        "Hot and Cold",
        "Cursed Ship",
        "Ice Castle",
        "Forgotten Island"
    }
elseif Sea3 then
    islandList = {
        "Port Town",
        "Great Tree",
        "Castle on the Sea",
        "Hydra Island",
        "Floating Turtle",
        "Haunted Castle",
        "Sea of Treats",
        "Tiki Outpost"
    }
end

for _, island in pairs(islandList) do
    Teleport:AddButton({
        Name = "Teleport to " .. island,
        Callback = function()
            -- Add teleport logic here
            OrionLib:MakeNotification({
                Name = "Teleporting",
                Content = "Teleporting to " .. island,
                Image = "rbxassetid://4483345998",
                Time = 3
            })
        end    
    })
end

-- ===================================================
-- MISC TAB CONTENT
-- ===================================================

Misc:AddSection({Name = "Miscellaneous"})

Misc:AddButton({
    Name = "Remove Fog",
    Callback = function()
        Lighting.FogEnd = 100000
        OrionLib:MakeNotification({
            Name = "Success",
            Content = "Fog Removed",
            Image = "rbxassetid://4483345998",
            Time = 3
        })
    end    
})

Misc:AddButton({
    Name = "Full Bright",
    Callback = function()
        Lighting.Brightness = 2
        Lighting.ClockTime = 14
        Lighting.GlobalShadows = false
        Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
        OrionLib:MakeNotification({
            Name = "Success",
            Content = "Full Bright Enabled",
            Image = "rbxassetid://4483345998",
            Time = 3
        })
    end    
})

Misc:AddToggle({
    Name = "Anti AFK",
    Default = true,
    Callback = function(Value)
        -- Anti-AFK is already enabled globally
    end    
})

-- ===================================================
-- SHOP TAB CONTENT
-- ===================================================

Shop:AddSection({Name = "Weapons"})

Shop:AddButton({
    Name = "Buy Katana",
    Callback = function()
        FireRemote("BuyItem", "Katana")
    end    
})

Shop:AddButton({
    Name = "Buy Cutlass",
    Callback = function()
        FireRemote("BuyItem", "Cutlass")
    end    
})

Shop:AddButton({
    Name = "Buy Pipe",
    Callback = function()
        FireRemote("BuyItem", "Pipe")
    end    
})

Shop:AddSection({Name = "Guns"})

Shop:AddButton({
    Name = "Buy Slingshot",
    Callback = function()
        FireRemote("BuyItem", "Slingshot")
    end    
})

Shop:AddButton({
    Name = "Buy Musket",
    Callback = function()
        FireRemote("BuyItem", "Musket")
    end    
})

Shop:AddButton({
    Name = "Buy Flintlock",
    Callback = function()
        FireRemote("BuyItem", "Flintlock")
    end    
})

Shop:AddButton({
    Name = "Buy Refined Flintlock",
    Callback = function()
        FireRemote("BuyItem", "Refined Flintlock")
    end    
})

Shop:AddButton({
    Name = "Buy Cannon",
    Callback = function()
        FireRemote("BuyItem", "Cannon")
    end    
})

Shop:AddButton({
    Name = "Buy Kabucha",
    Callback = function()
        FireRemote("BlackbeardReward", "Slingshot", "1")
        FireRemote("BlackbeardReward", "Slingshot", "2")
    end    
})

Shop:AddSection({Name = "Accessories"})

Shop:AddButton({
    Name = "Buy Black Cape",
    Callback = function()
        FireRemote("BuyItem", "Black Cape")
    end    
})

Shop:AddButton({
    Name = "Buy Swordsman Hat",
    Callback = function()
        FireRemote("BuyItem", "Swordsman Hat")
    end    
})

Shop:AddButton({
    Name = "Buy Tomoe Ring",
    Callback = function()
        FireRemote("BuyItem", "Tomoe Ring")
    end    
})

Shop:AddSection({Name = "Race"})

Shop:AddButton({
    Name = "Ghoul Race",
    Callback = function()
        FireRemote("Ectoplasm", "Change", 4)
    end    
})

Shop:AddButton({
    Name = "Cyborg Race",
    Callback = function()
        FireRemote("CyborgTrainer", "Buy")
    end    
})

-- ===================================================
-- VISUAL TAB CONTENT
-- ===================================================

local NotifiFruits = false
local NotifiTime = 15

-- Fruit Spawn Notifier
workspace.ChildAdded:Connect(function(part)
    if NotifiFruits then
        if part:IsA("Tool") or string.find(part.Name, "Fruit") then
            OrionLib:MakeNotification({
                Name = "Fruit Notifier",
                Content = "The fruit '" .. part.Name .. "' Spawned on the Map",
                Image = "rbxassetid://4483345998",
                Time = NotifiTime
            })
        end
    end
end)

Visual:AddSection({Name = "Notifications"})

Visual:AddSlider({
    Name = "Notification Time",
    Min = 5,
    Max = 120,
    Default = 15,
    Color = Color3.fromRGB(255,255,255),
    Increment = 1,
    ValueName = "Seconds",
    Callback = function(Value)
        NotifiTime = Value
    end    
})

Visual:AddToggle({
    Name = "Fruit Spawn",
    Default = false,
    Callback = function(Value)
        NotifiFruits = Value
    end    
})

Visual:AddSection({Name = "ESP"})

-- ESP Functions (Add your ESP logic here)
function EspFlowers()
    -- Add ESP logic for flowers
end

function EspPlayer()
    -- Add ESP logic for players
end

function EspFruits()
    -- Add ESP logic for fruits
end

function EspChests()
    -- Add ESP logic for chests
end

function EspIslands()
    -- Add ESP logic for islands
end

if Sea2 then
    Visual:AddToggle({
        Name = "ESP Flowers",
        Default = false,
        Callback = function(Value)
            getgenv().EspFlowers = Value
            EspFlowers()
        end    
    })
end

Visual:AddToggle({
    Name = "ESP Players",
    Default = false,
    Callback = function(Value)
        getgenv().EspPlayer = Value
        EspPlayer()
    end    
})

Visual:AddToggle({
    Name = "ESP Fruits",
    Default = false,
    Callback = function(Value)
        getgenv().EspFruits = Value
        EspFruits()
    end    
})

Visual:AddToggle({
    Name = "ESP Chests",
    Default = false,
    Callback = function(Value)
        getgenv().EspChests = Value
        EspChests()
    end    
})

Visual:AddToggle({
    Name = "ESP Islands",
    Default = false,
    Callback = function(Value)
        getgenv().EspIslands = Value
        EspIslands()
    end    
})

Visual:AddSection({Name = "Fake Stats"})

Visual:AddTextbox({
    Name = "Fake Defense",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        Player.Data.Stats.Defense.Level.Value = Value
    end      
})

Visual:AddTextbox({
    Name = "Fake Fruit",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        Player.Data.Stats["Demon Fruit"].Level.Value = Value
    end      
})

Visual:AddTextbox({
    Name = "Fake Gun",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        Player.Data.Stats.Gun.Level.Value = Value
    end      
})

Visual:AddTextbox({
    Name = "Fake Melee",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        Player.Data.Stats.Melee.Level.Value = Value
    end      
})

Visual:AddTextbox({
    Name = "Fake Sword",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        Player.Data.Stats.Sword.Level.Value = Value
    end      
})

Visual:AddSection({Name = "Fake Mode"})

Visual:AddTextbox({
    Name = "Fake Level",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        PlayerLevel.Value = Value
    end      
})

Visual:AddTextbox({
    Name = "Fake Points",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        Player.Data.Points.Value = Value
    end      
})

Visual:AddTextbox({
    Name = "Fake Bounty",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        Player.leaderstats["Bounty/Honor"].Value = Value
    end      
})

Visual:AddTextbox({
    Name = "Fake Energy",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        local plrEnergy = Player and Player.Character and Player.Character:FindFirstChild("Energy")
        if plrEnergy then
            plrEnergy.Max = Value
            plrEnergy.Value = Value
        end
    end      
})

Visual:AddTextbox({
    Name = "Fake Health",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        local plrHealth = Player and Player.Character and Player.Character:FindFirstChild("Humanoid")
        if plrHealth then
            plrHealth.MaxHealth = Value
            plrHealth.Health = Value
        end
    end      
})

Visual:AddTextbox({
    Name = "Fake Money",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        Player.Data.Beli.Value = Value
    end      
})

Visual:AddTextbox({
    Name = "Fake Fragments",
    Default = "",
    TextDisappear = false,
    Callback = function(Value)
        Player.Data.Fragments.Value = Value
    end      
})

-- ===================================================
-- BACKGROUND TASKS
-- ===================================================

task.spawn(function()
    local EffectContainer = ReplicatedStorage:WaitForChild("Effect", 9e9):WaitForChild("Container", 9e9)
    
    RunService.RenderStepped:Connect(function()
        local DeathEffect = EffectContainer:FindFirstChild("Death")
        
        if DeathEffect then
            DeathEffect:Destroy()
        end
    end)
    
    hookfunction(error, function() end)
    hookfunction(warn, function() end)
end)

-- ===================================================
-- INITIALIZE ORION
-- ===================================================

OrionLib:Init()

print("✓ NFC Scripts loaded successfully!")
print("✓ All tabs initialized!")
print("✓ Floating button ready!")
